Make start.sh executable, then run:

./kioku.sh <command> <filename-of-deck>
./kioku.sh gui sample

Refer to decks by their filename only, extension excluded.
Ex. if deck on path "~/Decks/sample.txt", use "sample"

Available commands:
- tui
- gui
- add
