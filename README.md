Welcome!

I experiment with Python here.

That is all.

Run scripts in the following order:

. install.sh      # once only, after cloning repo on a new machine
. setup.sh (name) # each time before running a specific program
python3 main.py   # to run a specific program
